import { borderRadiusScale, screenWidth } from '../../theme/metrics'

export const borderRadius = borderRadiusScale[4]
export const widthRatio = 0.82
export const heightRatio = 113 / 300
export const cardWidth = screenWidth * widthRatio
export const cardHeight = cardWidth * heightRatio
